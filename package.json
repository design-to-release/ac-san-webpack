{
  "name": "ac-san-webpack",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "install": "cd loader && npm i && cd .. && cd plugin && npm i && cd ..",
    "fmt": "dprint fmt",
    "fmt:check": "dprint check"
  },
  "dependencies": {
    "@swc/cli": "^0.1.46",
    "@swc/core": "^1.2.76"
  },
  "devDependencies": {
    "@types/webpack": "^5.28.0",
    "dprint": "^0.15.4"
  }
}
