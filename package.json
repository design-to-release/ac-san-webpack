{
  "name": "ac-san-webpack",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "postinstall": "cd loader && npm i && cd .. && cd plugin && npm i && cd ..",
    "fmt": "dprint fmt",
    "fmt:check": "dprint check"
  },
  "devDependencies": {
    "dprint": "^0.15.4"
  }
}
